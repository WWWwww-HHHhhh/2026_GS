% !TeX root = ../main.tex
\section{储能系统参数说明}
\label{app:storage_parameters}

\subsection{对照方案}

题目给出储能充放电效率为 \(90\%\)，但未分别说明
充电效率、放电效率与往返效率。
为考察不同解释对问题一结果的影响，设置以下两种方案。

主模型将充电效率与放电效率均取为 \(0.9\)，
对应往返效率为
\begin{equation}
    \eta_{\mathrm{round}}
    =\eta_c\eta_r
    =0.9\times0.9
    =0.81.
    \label{eq:app_efficiency_main}
\end{equation}
对照方案将题目中的 \(90\%\) 解释为整体往返效率，
并假设充放电效率相同，因此取
\begin{equation}
    \eta_c=\eta_r=\sqrt{0.9}\approx0.9487,
    \qquad
    \eta_{\mathrm{round}}=0.9.
    \label{eq:app_efficiency_alternative}
\end{equation}

两种方案使用相同的电价、负荷和光伏预测数据，
储能容量、充放电功率及日初日末储电量条件均保持不变，
仅调整状态转移方程中的效率参数。
分别求解完整的两阶段优化模型，
比较最终购电费用及充放电总量。

\subsection{计算结果与解释}

主模型的全天购电费用为 \(35126.95\) 元，
对照方案为 \(33801.50\) 元，如图~\ref{fig:app_efficiency} 所示。
往返效率由 \(81\%\) 提高至 \(90\%\) 后，
购电费用减少 \(1325.45\) 元，相对主模型下降约 \(3.77\%\)。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]
    {../All_Code/Q1/Results/Pictures/q1_efficiency_comparison.pdf}
    \caption{两种储能效率口径下的全天购电费用}
    \label{fig:app_efficiency}
\end{figure}

相应地，全天购电量由 \(59482.70\,\mathrm{kWh}\)
降至 \(57526.24\,\mathrm{kWh}\)，
充电量由 \(20740.66\,\mathrm{kWh}\)
降至 \(19842.71\,\mathrm{kWh}\)，
放电量由 \(16799.94\,\mathrm{kWh}\)
增至 \(17858.44\,\mathrm{kWh}\)。
这说明效率提高后，储能能够以更少的充电量提供更多可用电量，
从而减少补偿损耗所需的外网购电。

由于两种方案均要求日初和日末储电量相同，
全天储能损耗可由充放电总量之差计算
\begin{equation}
    E_{\mathrm{loss}}
    =
    E_{\mathrm{ch}}-E_{\mathrm{dis}}
    =
    \left(1-\eta_{\mathrm{round}}\right)E_{\mathrm{ch}}.
    \label{eq:app_storage_loss}
\end{equation}
主模型和对照方案的储能损耗分别约为
\(3940.73\,\mathrm{kWh}\) 和 \(1984.27\,\mathrm{kWh}\)。
效率提高减少了电量转移过程中的损耗，
为购电量和购电费用的下降提供了解释。

两种方案的储电量均满足
\(1200\,\mathrm{kWh}\) 至 \(10800\,\mathrm{kWh}\) 的运行范围，
日初和日末储电量均为 \(6000\,\mathrm{kWh}\)。
电量平衡和储能状态转移的最大绝对残差均小于
\(10^{-12}\,\mathrm{kWh}\)，
说明两组结果均满足相应模型约束。

\subsection{对主结论的影响}

无储能基准购电费用为 \(48052.05\) 元。
相较于该基准，主模型与对照方案的费用降幅分别为
\(26.90\%\) 和 \(29.66\%\)。
因此，效率口径会影响购电费用和充放电安排的具体数值，
但两种解释下均能通过储能调度降低购电费用，
未改变问题一关于储能经济性的主要结论。

本对照仅考察上述两种效率解释，
不据此推断其他效率范围内的敏感程度。
正文及正式结果文件仍采用充电效率和放电效率均为 \(0.9\)
的主模型结果。


\section{问题一模型检验的补充说明}
\label{app:q1_milp_check}

\subsection{等式约束残差}

将最终调度结果代回电量平衡方程与储能状态转移方程，定义残差为
\begin{equation}
\begin{aligned}
    e_t^{\mathrm{bal}}
    &=x_t^\ast+G_t-w_t^\ast+r_t^\ast-L_t-c_t^\ast,\\
    e_t^{\mathrm{sto}}
    &=s_t^\ast-s_{t-1}^\ast
      -\eta_c c_t^\ast+\frac{r_t^\ast}{\eta_r}.
\end{aligned}
\label{eq:q1_constraint_residuals}
\end{equation}

144个时段的最大绝对残差分别为
\[
    \max_t\left|e_t^{\mathrm{bal}}\right|
    =2.27\times10^{-13}\,\mathrm{kWh},
    \qquad
    \max_t\left|e_t^{\mathrm{sto}}\right|
    =8.31\times10^{-13}\,\mathrm{kWh}.
\]
因此，数值解在计算精度范围内满足两类等式约束。

储能荷电状态定义为
\begin{equation}
    \mathrm{SOC}_t
    =\frac{s_t^\ast}{12000}\times100\%.
    \label{eq:q1_soc_conversion}
\end{equation}
计算结果表明，全天SOC始终处于允许范围内。

\subsection{充放电互斥辅助检验}

引入二进制变量 $b_t^{\mathrm c}$ 和 $b_t^{\mathrm r}$，
分别表示第 $t$ 时段充电通道和放电通道是否启用，并设置
\begin{equation}
\begin{aligned}
    c_t&\leq q_{\max}b_t^{\mathrm c},\\
    r_t&\leq q_{\max}b_t^{\mathrm r},\\
    b_t^{\mathrm c}+b_t^{\mathrm r}&\leq1,
    \qquad
    b_t^{\mathrm c},b_t^{\mathrm r}\in\{0,1\}.
\end{aligned}
\label{eq:q1_milp_exclusivity}
\end{equation}

辅助模型保留原有电量平衡、储能状态转移、运行边界及日循环约束，并限制购电费用不超过
$C_1^\ast+\varepsilon$。

设 $v_t^{\mathrm c}$ 和 $v_t^{\mathrm r}$ 为通道状态变化变量，相邻时段状态变化的绝对值可线性化为
\begin{equation}
\begin{aligned}
    v_t^j&\geq b_t^j-b_{t-1}^j,\\
    v_t^j&\geq b_{t-1}^j-b_t^j,
    \qquad
    j\in\{\mathrm c,\mathrm r\},
    \quad t=2,\ldots,T.
\end{aligned}
\label{eq:q1_switch_linearization}
\end{equation}

辅助模型的目标函数为
\begin{equation}
    \min
    \sum_{t=2}^{T}
    \left(v_t^{\mathrm c}+v_t^{\mathrm r}\right)
    +
    10^{-6}\sum_{t=1}^{T}
    \left(b_t^{\mathrm c}+b_t^{\mathrm r}\right),
    \label{eq:q1_auxiliary_objective}
\end{equation}
其中，第一项用于减少通道状态切换，第二项用于抑制不必要的通道启用。

采用 HiGHS 求解后，辅助方案的购电费用为
$35126.948689$ 元，与原最低费用之差约为
$10^{-4}$ 元。充电和放电分别发生于45个和37个时段，通道启用状态共变化12次，说明增加互斥约束不会实质性损害原方案的经济性。

\section{问题二滚动求解与参数标定细节}
\label{app:q2_solution_details}

\subsection{CVaR线性化与场景费用}
\label{app:q2_cvar_settlement}

场景 $m$ 下的购电费用为
\begin{equation}
C_m
=
\sum_{t=1}^{T}\pi_t x_t
+
5\sum_{t=1}^{T}\pi_t e_t^m .
\label{eq:app_q2_scenario_cost}
\end{equation}

为将正文目标函数中的CVaR写成线性形式，引入风险阈值
$\zeta$ 和非负超额费用变量 $q_m$，并设置
\begin{equation}
q_m\geq C_m-\zeta,
\qquad
q_m\geq0,
\qquad m=1,\ldots,M .
\label{eq:app_q2_cvar_linearization}
\end{equation}
因此，在置信水平 $\alpha$ 下，场景费用的条件风险价值写为
\begin{equation}
\operatorname{CVaR}_{\alpha}(C)
=
\zeta+
\frac{1}{(1-\alpha)M}
\sum_{m=1}^{M}q_m .
\label{eq:app_q2_cvar}
\end{equation}
当目标函数最小时，$q_m$ 自动取
$\max\{C_m-\zeta,0\}$，从而度量场景费用超过风险阈值的部分。

\subsection{真实数据的逐时段结算}

日前模型求得计划购电量 $x_t$、计划充电量 $c_t$ 和计划放电量
$r_t$ 后，按照实际负荷和实际光伏进行逐时段结算。
储能充放电计划严格执行，不根据尚未实现的真实数据重新调整。

设时段 $t$ 扣除计划放电后的净需求为
\begin{equation}
D_t=L_t+c_t-r_t .
\label{eq:app_q2_net_demand}
\end{equation}
当 $D_t\geq0$ 时，依次使用光伏、计划购电和紧急购电满足需求：
\begin{equation}
\begin{aligned}
g_t&=\min\{G_t,D_t\},\\
y_t&=\min\{x_t,D_t-g_t\},\\
e_t&=\max\{0,D_t-g_t-y_t\},\\
w_t&=G_t-g_t .
\end{aligned}
\label{eq:app_q2_actual_settlement}
\end{equation}
其中，$g_t$ 为实际利用的光伏电量，$y_t$ 为计划电提取量，
$e_t$ 为紧急购电量，$w_t$ 为弃光量。

当 $D_t<0$ 时，说明固定放电产生的供给超过负荷与充电需求，
多余电量记为过剩弃电量
\begin{equation}
v_t=\max\{0,-D_t\}.
\label{eq:app_q2_supply_spill}
\end{equation}
由此，实际电量平衡满足
\begin{equation}
y_t+e_t+g_t+r_t
=
L_t+c_t+v_t .
\label{eq:app_q2_actual_balance}
\end{equation}

实际储电量按照已经确定的充放电计划更新：
\begin{equation}
s_t
=
s_{t-1}
+\eta_c c_t
-\frac{r_t}{\eta_r}.
\label{eq:app_q2_actual_soc}
\end{equation}
各时段计划购电按申报量全额计费，紧急购电按对应电价的5倍计费，
故实际日购电费用为
\begin{equation}
C^{\mathrm{actual}}
=
\sum_{t=1}^{T}\pi_t x_t
+
5\sum_{t=1}^{T}\pi_t e_t .
\label{eq:app_q2_actual_cost}
\end{equation}

结算时仅读取当前时段及此前已经实现的负荷和光伏数据，
不利用未来真实数据修改日前决策，因此不存在未来信息泄漏。
每日结算完成后，将日末储电量作为次日初始状态，实现全年储能状态的连续传递。

\subsection{线性规划形式与求解设置}

将计划购电、储能调度、场景电量分配、CVaR辅助变量和软终端变量
统一排列为决策向量 $\boldsymbol z$，每日调度模型可整理为
\begin{equation}
\begin{aligned}
\min_{\boldsymbol z}\quad
&\boldsymbol f^{\mathrm T}\boldsymbol z,\\
\mathrm{s.t.}\quad
&\boldsymbol A_{\mathrm{eq}}\boldsymbol z
=\boldsymbol b_{\mathrm{eq}},\\
&\boldsymbol A_{\mathrm{ub}}\boldsymbol z
\leq\boldsymbol b_{\mathrm{ub}},\\
&\boldsymbol l\leq\boldsymbol z\leq\boldsymbol u .
\end{aligned}
\label{eq:q2_standard_lp}
\end{equation}
其中，$\boldsymbol f$ 为目标函数系数向量；
$\boldsymbol A_{\mathrm{eq}}$ 和 $\boldsymbol A_{\mathrm{ub}}$
分别为等式和不等式约束矩阵；
$\boldsymbol l$ 和 $\boldsymbol u$ 为变量上下界。

场景电量分配、供需平衡、储能状态转移和软终端关系构成等式约束；
计划电提取上限及CVaR超额费用关系构成不等式约束；
储电量范围、充放电上限及变量非负性通过上下界设置。

当 $M=30$ 时，单日模型包含26530个变量、
13105条等式约束和4350条不等式约束。
程序采用稀疏矩阵存储约束，并通过 SciPy 调用 HiGHS
线性规划求解器。仅保留求解状态为最优，且等式约束最大绝对残差
和不等式约束最大违反量均不超过 $10^{-5}$ 的解。

\subsection{参数标定方法}
\label{app:q2_parameter_calibration}

为防止正式报告期数据参与模型选择，将2025年1月1日至14日设为预热期，
1月15日至31日设为验证期，2月1日至12月31日设为正式报告期。
预热期采用预先确定的基准参数运行，并将1月14日末的储电量作为
验证期的初始状态。

软终端系数的基准值取自问题一储能状态递推约束对应的边际价值。
问题一144个时段储能边际价值的中位数为
\begin{equation}
\kappa_{2,\mathrm{base}}=0.531667 .
\label{eq:app_q2_kappa_base}
\end{equation}
令
\begin{equation}
\kappa_2
=
\lambda\kappa_{2,\mathrm{base}},
\label{eq:app_q2_kappa_scaling}
\end{equation}
并在验证期内搜索以下参数组合：
\begin{equation}
\begin{aligned}
M&\in\{10,20,30\},\\
\beta&\in\{0,0.01,0.03,0.05,0.10,0.20,0.25,0.50\},\\
\lambda&\in\{0.25,0.50,1.00,2.00\}.
\end{aligned}
\label{eq:app_q2_parameter_candidates}
\end{equation}
置信水平固定为 $\alpha=0.90$。
基准组合设为
\[
M=20,\qquad \beta=0.25,\qquad \lambda=1.
\]

为避免候选方案通过消耗验证期末储能量获得虚假的费用优势，
将期末储能量损失按问题一储能边际价值折算，得到筛选得分
\begin{equation}
\mathcal S
=
C_{\mathrm{val}}
+
\kappa_{2,\mathrm{base}}
\left[s_{\mathrm{start}}-s_{\mathrm{end}}\right]_+ ,
\label{eq:app_q2_selection_score}
\end{equation}
其中，$C_{\mathrm{val}}$ 为验证期实际购电费用，
$[a]_+=\max\{a,0\}$。
首先保留紧急购电费用不高于基准组合的候选方案，
再从中选择筛选得分 $\mathcal S$ 最低的参数组合。

最终得到
\begin{equation}
M^\ast=30,\qquad
\beta^\ast=0.20,\qquad
\lambda^\ast=1,\qquad
\kappa_2^\ast=0.531667 .
\label{eq:app_q2_selected_parameters}
\end{equation}
该组合在验证期内的总费用为 $1016195.38$ 元，
紧急购电费用为 $16517.07$ 元；
相较基准组合分别降低 $59340.07$ 元和 $1497.35$ 元，
且验证期末储电量恢复至 $6000\,\mathrm{kWh}$。

参数在2025年1月31日完成选择，并在2月1日至12月31日的
334个正式调度日内保持不变。
报告期数据不参与参数选择。
在2025年12月31日设置
$s_T=6000\,\mathrm{kWh}$ 的硬约束，以保证全年储能状态闭合。

\section{问题二模型检验与局限性分析}
\label{app:q2_validation}

\subsection{因果性检验}

在预测和场景生成过程中，决策日 \(d\) 仅使用不晚于
\(d-1\) 日的历史数据，场景残差也全部来自决策日之前已经结束的日期。
1月预热期和验证期用于模型训练与参数选择，2月1日之后的正式报告期数据不参与参数标定，因此不存在未来信息泄漏。

官方结果模板与模型内部的自然日时段排列存在一列错位。
结果导出时仅根据已确定的时间映射调整计划购电量的填写位置，该操作不影响预测、场景生成和模型求解。
2025年12月31日末列对应2026年1月1日的首个时段，由于超出附件数据范围，因此保持为空。

\subsection{约束与结果复算}

将最终调度结果代回模型后，电量平衡约束的最大绝对残差为
\(2.27\times10^{-13}\,\mathrm{kWh}\)，储能状态转移约束的最大绝对残差为
\(9.09\times10^{-13}\,\mathrm{kWh}\)，相邻调度日的储电量衔接误差为零。
储电量边界、充放电功率、变量非负性和计划电提取上限均未出现违规。

根据逐时段购电结果重新计算计划购电费用与紧急购电费用，所得结果与程序输出完全一致。
模型结果与官方 \texttt{result2.xlsx} 的最大汇总差异为
\(6.05\times10^{-8}\)，说明模型求解、费用统计和结果导出之间保持一致。

为进一步检验储能的日内运行状态，统计334个正式调度日各时刻的储电量分布，结果如图~\ref{fig:q2_soc_distribution} 所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]
    {../All_Code/Q2_yy/Results/Pictures/fig_q2_soc_heatmap.pdf}
    \caption{报告期储电量分布及主要充放电时段}
    \label{fig:q2_soc_distribution}
\end{figure}

图中实线表示各时刻储电量的中位数，阴影区域表示
\(10\%\) 至 \(90\%\) 分位区间。
储能主要在凌晨和中午充电，并在早间和晚间集中放电。
各时刻储电量均位于
\(1.2\,\mathrm{MWh}\) 至 \(10.8\,\mathrm{MWh}\) 的运行范围内，进一步验证了储能边界约束的有效性。

\subsection{场景覆盖检验}

在固定随机种子后重新生成场景，所得数据与原场景的最大差异为
\(4.55\times10^{-13}\)，说明场景生成过程可以稳定复算。

报告期内，实际净负荷落入场景
\(5\%\) 至 \(95\%\) 分位区间的比例为
\(82.14\%\)，低于 \(90\%\) 的名义覆盖水平。
该结果表明，当前场景能够覆盖大部分净负荷波动，但对部分极端预测误差的刻画仍然不足。

在 \(M=30\) 的有放回抽样下，每日平均包含
\(19.22\) 个不同的历史残差块。
当置信水平为 \(\alpha=0.90\) 时，CVaR主要由约3个尾部场景决定，因此风险估计容易受到少量极端残差的影响。
报告期内共有51个调度日的实际费用高于模型当日CVaR，占全部调度日的
\(15.27\%\)。
该比例仅用于评价场景的样本外保守性，并非对CVaR约束的违规检验。

\subsection{模型局限性}

本模型在每日 \(0{:}00\) 确定全天计划购电和储能充放电量，真实数据到达后不再调整储能计划。
这一处理符合题目的日前决策要求，但在预测偏差较大时可能产生紧急购电或供给过剩弃电。

此外，30日滚动残差库对极端天气和长期季节变化的覆盖能力有限。
后续可在严格保持因果性的前提下扩大历史残差窗口，增加极端残差的抽样权重，或引入日内滚动修正机制，以提高场景覆盖能力并减少供需偏差造成的额外成本。

\section{问题三滚动优化的求解细节}
\label{app:q3_solution}

\subsection{线性规划的矩阵化}

设发布时刻 \(k\) 后剩余的时段数为
\(H_k=|\mathcal A_k|\)。
将该滚动窗口内的决策变量和场景变量排列为
\begin{equation}
\boldsymbol{\chi}_k
=
\left(
\widetilde{\boldsymbol{x}},
\boldsymbol{c},
\boldsymbol{r},
\boldsymbol{s},
\boldsymbol{z},
\xi_k^+,
\xi_k^-,
\zeta_k,
\left\{
\boldsymbol{y}^{\omega},
\boldsymbol{e}^{\omega},
\boldsymbol{g}^{\omega},
\boldsymbol{w}^{\omega},
\boldsymbol{h}^{\omega},
q_{k\omega}
\right\}_{\omega=1}^{M}
\right)^{\mathrm T}
\label{eq:app_q3_variable_vector}
\end{equation}
其中，\(\widetilde{\boldsymbol{x}}\)、\(\boldsymbol{c}\) 和
\(\boldsymbol{r}\) 为剩余时段的购电与储能决策，
\(\boldsymbol{s}\) 包含发布时刻的初始储电量及后续状态。
其余变量分别用于描述调整费用、终端偏差和各场景下的电量分配。

将目标函数、场景电量平衡和CVaR线性化关系写成矩阵形式，可得
\begin{equation}
\begin{aligned}
\min_{\boldsymbol{\chi}_k}\quad
&\boldsymbol{f}_k^{\mathrm T}\boldsymbol{\chi}_k \\
\mathrm{s.t.}\quad
&\boldsymbol{A}_{k,\mathrm{eq}}\boldsymbol{\chi}_k
=\boldsymbol{b}_{k,\mathrm{eq}} \\
&\boldsymbol{A}_{k,\mathrm{ub}}\boldsymbol{\chi}_k
\leq\boldsymbol{b}_{k,\mathrm{ub}} \\
&\boldsymbol{l}_k
\leq\boldsymbol{\chi}_k
\leq\boldsymbol{u}_k
\end{aligned}
\label{eq:app_q3_standard_lp}
\end{equation}
目标向量依次写入期望购电费用、CVaR、软终端惩罚和储能吞吐惩罚的系数。
场景电量平衡、光伏分配、储能状态转移和软终端关系构成等式约束。
调整费用线性化、计划电提取上限和CVaR超额费用关系构成不等式约束。
储电量范围、充放电上限及变量非负性通过上下界设置。

当 \(M=30\) 且 \(k=0\) 时，滚动窗口包含全天
\(H_0=144\) 个时段，线性规划共有22354个变量、8930条等式约束和4350条不等式约束。
随着日内时刻向后推进，剩余时段数逐渐减少，模型规模也随之下降。
为减少矩阵存储和计算开销，程序采用稀疏矩阵组织约束，并调用SciPy中的HiGHS线性规划求解器。

\subsection{日内滚动求解过程}

每日 \(0{:}00\) 以当日期初储电量为初始状态，并根据此时可获得的负荷与光伏预测生成30个联合场景。
求解全天随机规划模型后，得到零点计划购电量
\(\boldsymbol{x}^{0}\) 以及全天计划充放电量。

当策略规定的预报更新时刻到达后，依次执行以下步骤。

\begin{enumerate}[label=\arabic*.]
    \item 固定更新时刻之前已经执行的购电量、充电量和放电量。
    \item 读取当前时刻发布的光伏预报，并将其转换为剩余10分钟时段的预测电量。
    \item 以当前实际储电量作为新窗口的初始状态，重新生成负荷与光伏联合场景。
    \item 对尚未执行的时段重新求解线性规划，得到更新后的购电量、充电量和放电量。
    \item 将更新结果替换原方案中尚未执行的部分，已经执行的结果保持不变。
\end{enumerate}

各次调整均以当日 \(0{:}00\) 制定的
\(\boldsymbol{x}^{0}\) 作为费用结算基准，而不是相对上一次调整结果逐次累计。
因此，同一时段即使经过多次预报更新，最终只比较执行前生效的购电量
\(\widetilde{x}_t\) 与零点计划购电量 \(x_t^0\)。

对于一般调度日，日末储电量以当日期初储电量为软目标。
完成当天144个时段的实际结算后，将所得日末储电量传递为下一日的初始储电量。
在2025年12月31日的最后一个滚动窗口中，日末储电量被固定为
\(6000\,\mathrm{kWh}\)，从而保证全年状态闭合。

\subsection{真实数据的逐时段结算}

滚动模型求得购电量和储能计划后，按照实际负荷与实际光伏逐时段进行结算。
储能充放电量严格执行当前生效的计划，不利用当前时段之后的真实数据进行事后调整。

设第 \(t\) 个时段扣除储能放电后的用电需求为
\begin{equation}
D_t=L_t+c_t-r_t
\label{eq:app_q3_net_demand}
\end{equation}
当 \(D_t\geq0\) 时，依次使用光伏、计划购电和紧急购电满足需求
\begin{equation}
\begin{aligned}
g_t&=\min\{G_t,D_t\} \\
y_t&=\min\left\{\widetilde{x}_t,D_t-g_t\right\} \\
e_t&=\max\left\{0,D_t-g_t-y_t\right\} \\
w_t&=G_t-g_t
\end{aligned}
\label{eq:app_q3_actual_settlement}
\end{equation}
当 \(D_t<0\) 时，说明计划放电量超过负荷与充电需求。
此时多余电量记为供给过剩弃电量
\begin{equation}
h_t=\max\{0,-D_t\}
\label{eq:app_q3_spill}
\end{equation}
由此，各时段的实际电量平衡满足
\begin{equation}
y_t+e_t+g_t+r_t
=
L_t+c_t+h_t
\label{eq:app_q3_actual_balance}
\end{equation}

储电量根据实际执行的计划充放电量更新
\begin{equation}
s_t=s_{t-1}+\eta_c c_t-\frac{r_t}{\eta_r}
\label{eq:app_q3_actual_soc}
\end{equation}
由于储能计划在场景间共享并在实际运行中精确执行，因此储电量状态不随场景中的负荷和光伏变化。

第 \(t\) 个时段的实际费用由调整后的市场结算费用和紧急购电费用组成
\begin{equation}
C_t^{\mathrm{actual}}
=
\max\left\{
\frac{\pi_t}{2}\left(\widetilde{x}_t+x_t^0\right),
\frac{3\pi_t}{2}\widetilde{x}_t-\frac{\pi_t}{2}x_t^0
\right\}
+
5\pi_t e_t
\label{eq:app_q3_actual_cost}
\end{equation}
目标函数中的CVaR、软终端惩罚和储能吞吐惩罚只用于确定调度方案，不计入最终实际购电费用。

\subsection{策略回测与结果汇总}

正式回测范围为2025年2月1日至12月31日，共334个调度日。
对每种预报更新策略，均从
\(6000\,\mathrm{kWh}\) 的初始储电量出发，按照相同的负荷预测、场景生成、费用结算和跨日状态传递规则逐日运行。

程序分别保存每个时段的零点购电计划、最终购电计划、计划电提取量、紧急购电量、充放电量、弃光量、供给过剩弃电量和储电量。
随后按日汇总市场购电费用与紧急购电费用，并计算各策略在334个调度日内的实际总费用
\begin{equation}
C_{\mathrm{year}}
=
\sum_{i=1}^{334}
\sum_{t=1}^{144}
C_{i,t}^{\mathrm{actual}}
\label{eq:app_q3_annual_cost}
\end{equation}

最终在相同的数据和结算口径下比较六种策略的全年费用。
该过程使策略间的费用差异仅来源于预报更新时间及其引起的购电和储能计划调整。