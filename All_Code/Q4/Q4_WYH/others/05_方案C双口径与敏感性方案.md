# 方案 C：双口径并列 + 敏感性分析（执行说明）

> 目标：把"附件4 电价是否在 0:00 已知"这一**题面歧义**从"两半问互相矛盾"变成"一篇论文里的显式敏感性分析"。
> 纪律：**Q4_ZJY 与 Q3/Q2_yy 的既有代码一行不动**（Q3 是共享冻结层，其交接文档已标红"不要动"）；
> Q4_wyh 侧的新增全部走**新文件 + 向后兼容的可选参数**，主口径既有产物原样保留。

---

## 一、Q4_wyh 侧（已执行）

| 动作 | 文件 | 性质 |
| --- | --- | --- |
| 新增变体驱动（口径矩阵） | `Model_Establishment+Solution/q4_2_variants.py` | 新文件 |
| 场景源隔离 | `Data_processing/scenarios_M{10,20,30}_{q2,official}.pkl` | 新增副本，原 `scenarios_M*.pkl` 已还原为 q2 主口径 |
| 联络线容量可选约束 | `q4_core.py` 新增 `params["x_cap"]`（默认 `None` = 不启用，**行为与原先完全一致**） | 向后兼容 |
| 导出支持指定结果与输出 | `export_result4.py` 新增 `--results/--out/--verify-only`（默认值 = 原行为） | 向后兼容 |
| 变体结果落点 | `Data_processing/variants/<tag>_results.pkl`、`Results/Tables/<tag>_{tuning.xlsx,daily.csv,summary.csv}` | 与主口径产物隔离 |

### 口径矩阵（同一物理约束、同一结算规则，只切换两个维度）

| 维度 | 取值 | 含义 |
| --- | --- | --- |
| 价格信息 `--price-mode` | `unknown` | 电价 0:00 未知，用因果预测 + 逐场景价格（Q4_wyh 原主口径） |
| | `known` | 电价 0:00 已知（Q4_ZJY 口径），把当日实际电价复制到全部 M 个场景 |
| 光伏预报 `--pv-source` | `q2` | Q2_yy 自建因果预测，WAPE 0.066596（Q4_wyh 原主口径） |
| | `official` | 附件3 issue=0 + `linear_endpoint`，WAPE 0.083019（与 Q4_ZJY 同源） |

### 选参规则（P0-3 的修复项，两套并列运行以便隔离其影响）

| 规则 | 定义 |
| --- | --- |
| `legacy` | 沿用 Q2 协议：先剔除紧急购电费高于基准配置者，再取"验证期总费用 + κ 终端调整"最低 |
| `risk_aware` | **目标函数含 CVaR，选参规则也必须含风险项**：在"验证期总费用 ≤ 最小值×(1+1%) 且紧急购电费不高于基准配置"的候选里，取**验证期日费用 CVaR90 最低**者（并列取总费用更低者）。1% 容差是**显式写死**的，不随数据调整 |

> 说明：`risk_aware` 用"容差 + 尾部最小化"的两步式，而不是 `总费用 + λ·CVaR` 的加权式，是为了避免引入新的任意权重 λ。容差 1% 只用来把"费用上可接受"的候选圈出来，真正的排序量是 CVaR90。

### 运行中的四个变体

| 变体 tag | pv-source | price-mode | 用途 |
| --- | --- | --- | --- |
| `V_official_unknown` | official | unknown | **合规主口径候选**：用上题面点名的附件3 |
| `V_q2_unknown_risk` | q2 | unknown | **隔离选参规则的影响**（与现行主口径只差选参规则） |
| `V_official_known` | official | known | 对照口径（价格日前已知） |
| `V_q2_known` | q2 | known | 对照口径 + 原预报源，构成 2×2 |

另有 `V_cap8000`：主口径参数固定（M=30, β=0.2, κ×1.0），仅加"联络线容量 8000 kW"约束，用于 R-1。

### 结果矩阵（**已跑完**，数据源：`Results/Tables/V_*_summary.csv`，自动汇总见 `q4_2_variant_matrix.csv`）

| 变体 | 光伏预报源 | 价格信息 | 选参规则 | 选中 (M, β, κ×) | 报告期起点 SOC | 总费用(元) | 计划购电费(元) | 紧急购电费(元) | 紧急购电量(kWh) | 日费用 CVaR90(元) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 现行主口径（基线） | q2 | unknown | legacy | (30, 0.20, 1.0) | 6000 | 15,561,929.84 | 14,670,390.10 | 891,539.74 | 217,118.05 | 78,373 |
| `V_q2_unknown_risk` | q2 | unknown | **risk_aware** | (20, 0.00, 0.25) | 1200 | **15,444,173.44** | 14,345,859.01 | 1,098,314.43 | 267,394.64 | **76,786** |
| `V_official_unknown` | **official** | unknown | risk_aware | (20, 0.00, 0.25) | 1200 | 15,621,547.97 | 14,475,477.72 | 1,146,070.25 | 286,266.55 | 77,329 |
| `V_q2_known` | q2 | **known** | risk_aware | (30, 0.01, 0.25) | 1200 | **15,252,994.27** | 14,176,086.45 | 1,076,907.82 | 262,352.57 | **76,394** |
| `V_official_known` | official | **known** | risk_aware | (20, 0.01, 0.25) | 1200 | 15,479,035.55 | 14,332,130.46 | 1,146,905.09 | 286,511.95 | 76,890 |
| `V_cap8000`（R-1 敏感性） | q2 | unknown | legacy | (20, 0.20, 1.0) 固定 | 6000 | 15,553,009.61 | 14,594,881.90 | 958,127.71 | 234,659.87 | 77,150 |

### 派生结论（全部由上行数据直接相减得到，可复核）

| # | 结论 | 计算 | 数值 |
| --- | --- | --- | --- |
| C-1 | **价格信息的价值**（q2 源，同规则） | 15,444,173.44 − 15,252,994.27 | **+191,179.17 元（+1.25%）** |
| C-2 | **价格信息的价值**（official 源，同规则） | 15,621,547.97 − 15,479,035.55 | **+142,512.42 元（+0.92%）** |
| C-3 | **选参规则修复效果**（同 pv/price） | 15,561,929.84 − 15,444,173.44 | **−117,756.40 元（−0.76%）**，且 CVaR90 78,373 → 76,786 |
| C-4 | **附件3 官方预报的成本**（同价格口径 unknown） | 15,621,547.97 − 15,444,173.44 | **+177,374.53 元（+1.15%）** |
| C-5 | **附件3 官方预报的成本**（同价格口径 known） | 15,479,035.55 − 15,252,994.27 | **+226,041.28 元（+1.48%）** |
| C-6 | **R-1 联络线容量 8000 kW 的影响** | 15,553,009.61 − 15,561,929.84 | **−8,920.23 元（−0.06%）** |
| C-7 | **风险感知口径 vs B1（Q2 固定价策略）** | 15,499,485.65 − 15,444,173.44 | 主模型现**低于 B1 55,312.21 元**（P0-3 的连带问题消除） |

### 关于"报告期起点 SOC 不一致"的诚实说明（必须写进论文脚注）

| 变体 | 起点 SOC |
| --- | --- |
| 现行主口径、`V_cap8000` | 6000.0 kWh |
| `V_q2_unknown_risk`、`V_official_unknown`、`V_q2_known`、`V_official_known` | **1200.0 kWh** |

原因：SOC 必须从题目给定的 2025-01-01 00:00 = 6000 kWh **连续传递**，而调参期（1/15~1/31）的充放电由当日模型决定；选中 κ×0.25 的配置会把 1 月末 SOC 拉到 1200。各变体报告期末均被硬约束拉回 6000 kWh（`final_soc` = 6000）。
- 混淆量级：起点低 4,800 kWh ⇒ 报告期内需净补 4,800 kWh ⇒ 按 η=0.9、均价 ≈0.64 元/kWh 计约 **3,413 元（0.022%）**，比 C-1~C-5 所测效应小 **35~66 倍**，故不影响结论方向与量级。
- 若要彻底消除该混淆，需把各变体的报告期起点统一钉在 6000 kWh 重跑（一次约 8 分钟/变体）；**属于可选精修，不是结论成立的前提**。

### 建议采纳口径（供拍板）

| 用途 | 建议 | 理由 |
| --- | --- | --- |
| **论文主口径** | `V_q2_unknown_risk`（q2 预报 + 价格未知 + 风险感知选参） | 费用最低的合规组合之一（15,444,173 元），CVaR90 也更低，且**低于 B1**，把"波动电价建模的价值"重新立住 |
| 论文对照口径 | `V_q2_known`（价格 0:00 已知） | 量化"价格信息价值 = 191,179 元（1.24%）"，直接把题面歧义写成敏感性 |
| 合规性敏感性 | `V_official_unknown` / `V_official_known` | 说明"若严格使用题面点名的附件3 预报，费用上升 1.15%~1.48%"，把取舍讲透 |
| 提交文件 | 现有 `result4-2.xlsx` **暂不覆盖**；已另存 `Results/Tables/result4-2_风险感知选参.xlsx`（8/8 回读校验 PASS） | 一旦拍板采纳，执行下面一条命令即可切换正式提交件 |

```bash
# 采纳风险感知口径作为正式提交件（覆盖 result4-2.xlsx）
python Model_Establishment+Solution/export_result4.py \
  --results Data_processing/variants/V_q2_unknown_risk_results.pkl \
  --out Results/Tables/result4-2.xlsx
```


---

## 二、Q4_ZJY 侧（**改动规格，建议由该工作区负责人执行**）

### 为什么不由我直接改

1. Q4_ZJY 全链 `import` `All_Code/Q3/Model_Establishment+Solution/{q3_core,run_q3,build_result3}`，其 `HANDOVER.md` 明确标红"**不要动 Q3 下任何文件**"——改了会同时污染 Q3 已定稿结果。
2. 因此**正确做法是在 Q4_ZJY 内新增一层价格场景包装**，而不是修改 `q3_core.py`。

### 具体改动规格（三步，全部为新增文件）

1. **新增 `Q4_ZJY/Data_processing/q4_price_scenarios.py`**
   直接复用 Q4_wyh 已跑通的两份产物（跨工作区只读引用，不复制逻辑）：
   - `Q4_wyh/Data_processing/price_forecast.pkl` → `price_hat`（144×365，因果预测）
   - `Q4_wyh/Data_processing/scenarios_M{M}_q2.pkl` → `P_all`（365×M×144，逐场景价格路径）
   输出：把 `P_all[:, day, :]` 转成 `(M,144)` 供当日求解。

2. **新增 `Q4_ZJY/Model_Establishment+Solution/solve_window_scen.py`**
   把 `q3_core.solve_window` 复制到本地并做**三处**价格场景化（与 Q4_wyh 的 `q4_core.py` 对 Q2 优化器的改动**完全同构**）：
   - 目标中 `x` 的系数：由 `price[t]` → 场景期望 `mean_m price[m,t]`；
   - 目标中紧急购电 `e` 的系数：由 `5·price[t]` → 逐场景 `5·price[m,t]`；
   - CVaR 线性化约束 `q ≥ C^ω − ζ` 内的价格系数：逐场景 `price[m,t]`。
   其余（变量布局、约束、`execute_slot`、结算）**一行不改**。
   ⚠️ 该策略下 6:00/12:00/18:00 的更新还需要"已实现价格 + 剩余时段因果预测"的拼接（Q4_wyh 的 `04_q4_price_forecast.py` 已产出全天 `price_hat`，可直接按时刻切片使用）。

3. **新增 `Q4_ZJY/run_q4_scenarios.py`**：与 `run_q4.py` 相同的六策略回测，只是把 `solve_window` 换成第 2 步的场景版；输出到 `Results/Tables/price_unknown/{S0..Sall}/` 与 `result4-3_price_unknown.xlsx`。

### 交付形态（方案 C 的最终样子）

| 工作区 | 主口径 | 对照口径 |
| --- | --- | --- |
| Q4_wyh（4-2） | 价格未知（场景 + CVaR） | 价格 0:00 已知 |
| Q4_ZJY（4-3） | 价格 0:00 已知（现状，不动） | 价格未知（上述三步新增） |

论文写法（建议原句）：
> 附件4 未声明电价的发布时点。为不把这一歧义埋进结论，本文在**同一套物理约束与结算规则**下，对"电价在 0:00 是否已知"给出两种信息假设并分别求解：结果见表 X。两者之差即为**价格信息的价值**，其量级为 ___ 元（___%），小于负荷/光伏预测误差带来的信息成本，故该歧义不影响本文的主要结论。

---

## 三、4~6 项的文字与敏感性落点

### R-1 联络线容量（Q4_wyh 论文必备假设条文）

> 题目仅规定了储能设备的充放电功率上限（5000 kW），未规定微网与外部电网之间的联络线容量。因此模型不对计划购电功率施加额外约束，其单时段上限由"小区负载 + 储能充电功率上限"共同决定。为检验该设定的影响，本文另以联络线容量 8000 kW 重新求解对照（见 R-1 敏感性表）。

敏感性入口：`python q4_2_variants.py --pv-source q2 --price-mode unknown --select legacy --report-only-beta 0.2 --report-only-kappa 1.0 --import-cap-kw 8000 --tag V_cap8000`

### R-2 未提取计划电量（必须主动披露）

> 按题面"除紧急购电费用外，其他时间段的购电费用均按计划购电量计算"，计划购电量一经确定即需付费，而实际提取量以计划量为上限。该口径并非取巧：本文实测 334/334 天均出现"计划量超过当日实际需要"，累计未提取 1,639,609.3 kWh（占计划量 7.19%），因此"计划量必须全额提取"（y≡x）在因果执行下**不可行**。未提取量是 5 倍紧急购电价下的最优对冲结果（边际上"多计划 1 kWh 付 λ、少计划 1 kWh 付 5λ"，临界比 5/6）。

### R-3 SOC 终端口径（两半问必须用同一句）

> 储能终端采用软约束：以题目给定的 2025-01-01 00:00 储电量 6000 kWh 为基准，逐日对"日末储电量偏离日初值"施加 κ 惩罚（κ 由 Q1 储能边际价值中位数标定，κ=0.5317）；仅报告期最后一天（2025-12-31 24:00）施加硬约束回到 6000 kWh。因此个别日期日末储电量可与日初不同（实测 Q4-2 有 3/334 天偏离超过 1 kWh，最大 4,800 kWh）。

> ⚠️ Q4_ZJY 的 `HANDOVER.md` §3.2 写作"日末回到日初值"，与本文一手数据解析结果（53/334 天偏离 >1 kWh）不一致，**需以代码为准统一表述**。

### R-4 Q4_ZJY 采纳策略措辞与数字统一

- 统一数字：S0 − S6_12 = 15,499,059.00 − 15,137,156.79 = **361,902.21 元**（`README.md` 里的 360,920 是拿 Sall 算的，已由 `HANDOVER.md` §4.3 更正，论文请用 361,902）。
- 措辞降级（避免把 0.0065% 的差距说成结论）：

> 在两种电价制度下，"引入 6:00/12:00 更新、不引入 18:00 更新"的结论一致；其中 18:00 更新的边际收益在波动电价下降至 981.73 元（占全年费用的 0.0065%），不具统计意义，故采纳更新次数更少的 S6_12 以降低执行复杂度，而非断言 18:00 更新"有害"。

---

## 四、落地后必须重新通过的复核

1. 数据核验 25/25（`q4_data_audit.csv`）；
2. 结构核验 9/9（`q4_2_structural_checks.csv`）；
3. 工作簿回读 8/8（`q4_2_export_verify.md`），Q4_ZJY 侧对应 `validate_result4_workbook.py`；
4. **口径对账表**（两边填同一张）：电价源 / 日期对齐 / 报告期 / 参数 / 终端 SOC / 时间映射 / 预报源 / 结算规则 / 价格信息假设。
